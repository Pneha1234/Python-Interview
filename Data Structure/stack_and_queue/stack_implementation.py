# stack implementation using arrays
# LIFO(Last in First Out)
# fix a pointer called top = -1, now push(6), push(3), push(7)
#[6,3,7]
# to push, top =top +1 array[top] = element
# to pop(), top = top-1
# top = array[top]
# size = top +1
#is_empty = if top == -1